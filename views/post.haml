=haml :single_post, :locals => { :post => post }, :layout => false
-if logged_in?
  .admin
    Admin:
    %a{ :href => "/edit_post/#{post.id}" } Edit This Post
- if ALLOW_COMMENTS
  #comments
    =haml :disqus_embed, :layout => false

    