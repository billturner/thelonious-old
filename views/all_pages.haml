%h2= @page_title
%ul
  -Page.all.each do |page|
    %li
      =link_to page.title, page_url(page)
      &mdash;
      =page.created_at.strftime('%Y-%m-%d %I:%M%p')
      \:
      =link_to("Edit &raquo;", "/edit_page/#{page.id}")
