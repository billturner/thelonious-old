%h2= @page_title
%h3 Draft Posts
%ul
  -Post.all_drafts.each do |post|
    %li
      =post.title
      &mdash;
      =post.created_at.strftime('%Y-%m-%d %I:%M%p')
      (
      %a{ :href => "/edit_post/#{post.id}" }> Edit
      )
%h3 Published Posts
%ul
  -Post.all_published.each do |post|
    %li
      %a{ :href => permalink_url(post) }= post.title
      &mdash;
      =post.published_at.strftime('%Y-%m-%d %I:%M%p')
      (
      %a{ :href => "/edit_post/#{post.id}" }> Edit
      )