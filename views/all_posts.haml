%h2= @page_title
%h3 Draft Posts
%ul
  -Post.all_drafts.each do |post|
    %li
      =post.title
      &mdash;
      =post.created_at.strftime('%Y-%m-%d %I:%M%p')
      \:
      =link_to "Edit", "/edit_post/#{post.id}"
%h3 Published Posts
%ul
  -Post.all_published.each do |post|
    %li
      =link_to post.title, permalink_url(post)
      &mdash;
      =post.published_at.strftime('%Y-%m-%d %I:%M%p')
      \:
      =link_to "Edit", "/edit_post/#{post.id}"
